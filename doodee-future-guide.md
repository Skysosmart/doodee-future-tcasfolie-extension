# Doodee future — คู่มือสร้าง Chrome Extension สำหรับ TCASFolio

## เป้าหมาย

ช่วยกรอกแฟ้มสะสมผลงานใน TCASFolio ให้เร็วขึ้น โดยดึงเนื้อหาจากเล่ม portfolio เดิมที่พิมพ์เก็บไว้ครั้งเดียว แล้วนำกลับมาใช้ซ้ำได้ทุกสาขาวิชา (ระบบรองรับได้สูงสุด 40 แฟ้ม)

---

## หลักการที่ต้องยึด

**1. ห้าม auto-fill ฟอร์มจริงในเฟสแรก**
ฟอร์มเป็น framework-based (React/Vue) การ set `input.value` ตรง ๆ จะไม่ trigger event ของ framework → กดบันทึกแล้วข้อมูลหาย
ถ้าจะทำจริงต้องใช้ native setter:
```js
const setter = Object.getOwnPropertyDescriptor(
  window.HTMLTextAreaElement.prototype, "value"
).set;
setter.call(el, text);
el.dispatchEvent(new Event("input", { bubbles: true }));
```
แต่ยังเสี่ยงอยู่ดี — นี่คือใบสมัครจริง เฟสแรกใช้ **copy to clipboard** แล้วให้ผู้ใช้วางเอง

**2. ข้อมูลอยู่ในเครื่องเท่านั้น**
ใช้ `chrome.storage.local` ห้ามยิงข้อมูลออก network ทุกกรณี หน้านี้มีข้อมูลส่วนตัวจริง (เลขบัตร ผลการเรียน คะแนน verified)

**3. อย่าแตะข้อมูลที่ verified**
ข้อมูลที่มีเครื่องหมาย Verify มาจากหน่วยงานต้นทาง extension ควรอ่านอย่างเดียว

---

## โครงสร้างไฟล์

```
doodee-future/
├── manifest.json
├── popup.html          # คลังผลงาน — เพิ่ม / ดู / คัดลอก
├── popup.js
├── content.js          # แถบด้านข้างบนหน้า TCASFolio
├── content.css
└── storage.js          # ฟังก์ชันอ่าน/เขียน storage ใช้ร่วมกัน
```

### manifest.json (เป้าหมาย)

```json
{
  "manifest_version": 3,
  "name": "Doodee future",
  "version": "1.0",
  "permissions": ["storage"],
  "host_permissions": ["https://student.mytcas.com/*"],
  "action": { "default_popup": "popup.html" },
  "content_scripts": [
    {
      "matches": ["https://student.mytcas.com/*"],
      "js": ["content.js"],
      "css": ["content.css"],
      "run_at": "document_idle"
    }
  ]
}
```

---

## Data model

```js
{
  id: "uuid",
  type: "รางวัล | โครงงาน | กิจกรรม | อบรม | ผลงานสร้างสรรค์",
  title: "ชื่อผลงาน",
  org: "หน่วยงาน / ปี",
  detail: "รายละเอียดแบบเต็ม",
  tags: ["วิศวะ", "คอม"],     // ไว้กรองตามสาขาที่สมัคร
  createdAt: 1234567890
}
```

เก็บทั้งหมดเป็น array ใต้ key เดียว `folioItems`

---

## เฟสการทำ

### เฟส 1 — คลังผลงาน (ไม่ต้องพึ่งหน้าเว็บเลย)
- popup: ฟอร์มเพิ่มผลงาน + รายการที่บันทึกไว้
- ปุ่มคัดลอกรายละเอียดลง clipboard
- ปุ่มลบ / แก้ไข
- export / import เป็นไฟล์ JSON (กัน storage หาย)

### เฟส 2 — แถบด้านข้างบนหน้า TCASFolio
- content script inject panel ลอยมุมขวา (`position: fixed`, `z-index` สูง)
- แสดงคลังผลงาน กรองตาม type และ tag
- ปุ่มคัดลอกทีละชิ้น
- ปุ่มย่อ/ขยาย panel จำสถานะไว้ใน storage

**ข้อควรระวัง:** หน้าเว็บ render ทีหลัง content script ต้องใช้ `MutationObserver` รอ element ที่ต้องการ

```js
const observer = new MutationObserver(() => {
  const target = document.querySelector("SELECTOR_ที่หาได้จาก_DevTools");
  if (target && !document.getElementById("doodee-future-panel")) {
    injectPanel();
  }
});
observer.observe(document.body, { childList: true, subtree: true });
```

### เฟส 3 — ของแถมที่มีประโยชน์จริง
- **แบนเนอร์เตือนก่อนแก้ไข** — ทุกครั้งที่แก้ไข ไฟล์ PDF และลิงก์จะเปลี่ยนใหม่ ถ้าส่งลิงก์ให้มหาลัยไปแล้วเขาจะเห็นแค่เวอร์ชันเดิม ให้ทำเครื่องหมายแฟ้มที่ส่งไปแล้ว แล้วขึ้นเตือนตัวใหญ่ ๆ ตอนเปิดแก้
- backup ข้อความที่กำลังพิมพ์ลง storage อัตโนมัติทุก 5 วินาที (อ่านอย่างเดียว ไม่เขียนกลับฟอร์ม)

---

## วิธีหา selector

1. เปิดหน้าฟอร์มจริง
2. คลิกขวาที่ช่องที่ต้องการ → Inspect
3. ดู class / id ที่ไฮไลต์ใน DevTools
4. ทดสอบใน Console ก่อนเขียนลงโค้ด: `document.querySelectorAll("...")` ต้องได้ผลลัพธ์ตามที่คิด
5. เลี่ยง class ที่ดูเป็น hash สุ่ม (เช่น `css-1x2y3z`) เพราะเปลี่ยนทุก build — ใช้ `aria-label`, `name`, `placeholder`, หรือโครงสร้าง DOM แทน

---

## เช็กก่อนคิดฟีเจอร์ใหม่ (ระบบมีให้แล้ว)

- นับคำ / นับตัวอักษรในเรียงความ — มีแล้ว แบบเรียลไทม์
- แจ้งเตือนกรอกข้อมูลสำคัญไม่ครบ — มีแล้ว
- ดาวน์โหลด PDF / คัดลอกลิงก์ — มีแล้ว

---

## ทดสอบ

- ปิด-เปิดเบราว์เซอร์แล้วข้อมูลต้องยังอยู่
- ใส่ข้อความยาว ๆ และภาษาไทยผสมอังกฤษ ต้องไม่เพี้ยน
- โหลดหน้า TCASFolio ใหม่หลาย ๆ รอบ panel ต้องไม่ inject ซ้ำซ้อน
- ลอง export → ลบ storage → import กลับ ต้องได้ข้อมูลเดิมครบ

---

## สถานะ (20 ส.ค. 2569)

- **เฟส 1 — เสร็จแล้ว** เพิ่ม / แก้ไข / ลบ / แท็ก / คัดลอก / export / import JSON
- **เฟส 2 — เสร็จแล้ว** panel บน `student.mytcas.com` กรองตามประเภทและแท็ก
  คัดลอกทีละชิ้น ย่อ/ขยายได้และจำสถานะ
- **เฟส 3 — ทำแล้วบางส่วน** เติมข้อมูลลงช่องที่เลือกไว้ได้เลย ไม่ต้อง copy-paste
  (ดูหัวข้อถัดไป) ส่วน "กรอกทั้งฟอร์มรวดเดียว" ยังไม่ทำ ต้องเปิดหน้าจริงหา selector ก่อน

### เติมลงช่องที่เลือก (เฟส 3)

**วิธีใช้: คลิกช่องในฟอร์ม TCASFolio ก่อน แล้วค่อยกดปุ่มใน panel**

ทุกผลงานจะมีปุ่มเล็ก ๆ สามปุ่ม — `ชื่อ` `หน่วยงาน` `รายละเอียด` — กดอันไหน
ข้อมูลชิ้นนั้นก็ลงไปในช่องที่เพิ่งคลิกไว้

จงใจไม่ให้มันเดาเองว่าช่องไหนคือช่องอะไร คนเลือกช่อง extension แค่เติม
แปลว่าเว็บเขาจะเปลี่ยนหน้าตากี่รอบก็ยังใช้ได้ ไม่ต้องมาแก้ตาม

กติกาที่ใส่ไว้ (ทดสอบแล้วทุกข้อ):
- ช่องที่เป็น readonly / disabled — **ไม่แตะ** ขึ้นข้อความบอกแทน (นี่คือด่านกัน
  ข้อมูลที่ verify แล้ว เท่าที่ markup ของเว็บเขาจะบอกได้)
- ช่องรหัสผ่าน / อัปโหลดไฟล์ / checkbox ฯลฯ ไม่เป็นเป้าหมายเลย
- ไป focus ช่องอื่นที่เติมไม่ได้ มันจะ**ลืม**ช่องเก่าทิ้ง ไม่ไปเติมผิดที่
- ช่องที่มีข้อความอยู่แล้ว ต้องกดยืนยันซ้ำ (ปุ่มจะเปลี่ยนเป็น `ทับของเดิม?`)
- เติมแล้วมีปุ่ม **ย้อนกลับ** เสมอ คืนค่าเดิมได้เป๊ะ
- **ไม่กดปุ่มส่งฟอร์มให้เด็ดขาด** ไม่ว่ากรณีไหน

เหตุผลที่คู่มือเดิมห้ามเรื่องนี้ไว้เป็นเรื่องจริง: การเซ็ต `input.value` ตรง ๆ
กับฟอร์ม React ข้อความจะขึ้นบนจอแต่ตอน submit เป็นค่าว่าง ตัวนี้เลยใช้ native
setter แล้วยิง `input` event ให้ React รับรู้เหมือนคนพิมพ์จริง — ทดสอบกับฟอร์ม
จำลองที่เก็บ state แยกจาก DOM แล้ว ค่าที่ "จะถูกส่งจริง" ตรงกับที่เติมทุกตัว

### panel เริ่มที่ย่อไว้ — ตั้งใจให้เป็นแบบนั้น

panel เป็น `position: fixed` ชิดขวา z-index สูงสุด ตอนกางมันจะกินพื้นที่กว้าง
ประมาณ 300px สูงถึง 70% ของจอ **และคลิกทะลุลงไปที่ปุ่มข้างหลังไม่ได้**
(วัดด้วย `elementFromPoint` แล้ว ปุ่มที่อยู่ใต้ panel จะกดไม่ติด โดยไม่มีอะไรบอก)
ถ้าปุ่มของใบสมัครจริงอยู่ตรงนั้นพอดีจะกลายเป็นเรื่องใหญ่ จึงให้เริ่มที่ย่อไว้ก่อน
กดลูกศรเพื่อกาง แล้วมันจะจำไว้ให้เอง

**พูดให้ตรง: การย่อคือการลดพื้นที่ทับ ไม่ใช่การแก้ให้หายทับ** ตอนย่อมันยังกิน
พื้นที่คลิกอยู่ประมาณ 46×42 px และย้ายไปนั่งกลางขอบขวา (`top: 50%`) เพื่อให้พ้น
มุมบนขวาซึ่งเป็นที่ของเมนูบัญชี/ปุ่มออกจากระบบ ถ้าเว็บจริงดันมีปุ่มตรงกลางขอบขวาพอดี
ก็ยังทับได้อยู่ดี

ถ้าเจอว่าหน้าไหนกดปุ่มไม่ติด — **ย่อ panel ก่อน** แล้วลองใหม่ ถ้ายังไม่ติดอีก
ให้ปิดส่วนขยายชั่วคราวที่ `chrome://extensions` แล้วค่อยกลับมาเปิด

อยากให้กางตั้งแต่แรก แก้ `return true` บรรทัดเดียวใน `getPanelCollapsed` ที่ `storage.js`

### ทดสอบแล้วจริงบน Chromium 151

- ปิด-เปิดเบราว์เซอร์ทั้งตัว ข้อมูลยังอยู่ครบ ✓
- ข้อความยาว 405 ตัวอักษร ไทยผสมอังกฤษหลายย่อหน้า เก็บและคัดลอกออกมาตรงเป๊ะ ✓
- โหลดหน้าใหม่ 5 รอบ panel inject ครั้งเดียวทุกรอบ ✓
- export → `chrome.storage.local.clear()` → import กลับ ได้ครบทั้ง id, แท็ก,
  `createdAt` และรายละเอียด ✓
- css ของหน้าเว็บแก้หน้าตา panel ไม่ได้ และ css ของ panel ไม่รั่วไปโดนฟอร์ม ✓
- ค่าในฟอร์มจำลองไม่ถูกแตะเลยหลัง panel ขึ้น ✓

ไฟล์ที่เพิ่มจากคู่มือเดิม: `model.js` (ตรรกะล้วน เทสต์ด้วย `npm test`) แยกจาก
`storage.js` (ตัวต่อกับ chrome.storage) เหตุผลและสิ่งที่ต่างจากคู่มือนี้อยู่ใน
`docs/superpowers/specs/2026-08-18-doodee-future-design.md`

ตอนแพ็กเป็น .zip ให้ตัด `test/`, `package.json`, `docs/`, `.git/` ออก
